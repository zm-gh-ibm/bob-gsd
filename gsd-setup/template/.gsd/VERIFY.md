# VERIFY

<!-- Verification reports, appended by gsd-verifier (one dated section per phase, never
     overwritten — see .bob/rules-gsd-verifier/03-history-preservation.md). Empty until the
     first phase is verified. -->