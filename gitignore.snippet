# bob rewrites this file on every commit, which re-dirties the working tree right
# after the Shipper commits workflow state. Ignore it so the clean-tree gate stays
# green (see .bob/rules-gsd-shipper/06-commit-workflow-state.md).
.bob/notes/
